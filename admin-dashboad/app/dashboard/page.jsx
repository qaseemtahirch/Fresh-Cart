'use client';import {useEffect,useState} from 'react';import AdminShell from '@/components/AdminShell';import {api} from '@/lib/api';import {ShoppingCart,Users,Package,Wallet} from 'lucide-react';export default function Dashboard(){const [d,setD]=useState(null),[err,setErr]=useState('');useEffect(()=>{api('/admin/dashboard').then(setD).catch(e=>setErr(e.message))},[]);const s=d||{};return <AdminShell title="Dashboard">{err&&<div className="error">{err}</div>}<div className="grid stats"><Stat icon={ShoppingCart} label="Orders Today" value={s.orders_today??'—'}/><Stat icon={Wallet} label="Revenue Today" value={s.revenue_today!=null?`Rs. ${Number(s.revenue_today).toLocaleString()}`:'—'}/><Stat icon={Package} label="Products" value={s.products??'—'}/><Stat icon={Users} label="Active Riders" value={s.active_riders??'—'}/></div><div className="grid two section"><div className="card"><div className="sectionHead"><h2>Store overview</h2><span className="pill green">Live</span></div><p className="muted">FreshCart admin is connected to the Go backend. Use the left menu to manage catalog, orders, delivery and discounts.</p></div><div className="card"><h2 style={{fontSize:17}}>Quick actions</h2><div className="toolbar" style={{marginTop:14}}><a className="btn primary" href="/products">Add product</a><a className="btn secondary" href="/coupons">Create coupon</a><a className="btn secondary" href="/orders">View orders</a></div></div></div></AdminShell>};function Stat({icon:Icon,label,value}){return <div className="card stat"><div><div className="muted">{label}</div><div className="big">{value}</div></div><div className="icon"><Icon size={21}/></div></div>}
