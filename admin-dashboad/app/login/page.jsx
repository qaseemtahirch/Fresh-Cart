'use client';import {useState} from 'react';import {api} from '@/lib/api';export default function Login(){const [email,setEmail]=useState('admin@freshcart.local'),[password,setPassword]=useState('FreshCart123!'),[error,setError]=useState('');async function submit(e){e.preventDefault();setError('');try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});if(d.user?.role!=='admin')throw Error('Admin account required');localStorage.setItem('freshcart_token',d.token);localStorage.setItem('freshcart_user',JSON.stringify(d.user));location.href='/dashboard'}catch(e){setError(e.message)}}return <main className="login"><form className="loginBox" onSubmit={submit}><div className="brand" style={{padding:'0 0 20px',color:'#0b3d26'}}>Fresh<span>Cart</span> <small>ADMIN</small></div><h1>Welcome back</h1><p className="muted">Sign in to manage your store.</p>{error&&<div className="error">{error}</div>}<div className="field" style={{marginTop:18}}><label>Email</label><input className="input" style={{width:'100%'}} value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field" style={{marginTop:14}}><label>Password</label><input type="password" className="input" style={{width:'100%'}} value={password} onChange={e=>setPassword(e.target.value)}/></div><button className="btn primary" style={{width:'100%',marginTop:20}}>Sign in</button></form></main>}
